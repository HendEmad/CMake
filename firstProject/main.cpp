#include<bits/stdc++.h>

template <typename T>
T add(T a, T b)
{
    return a + b;
}

int main(){
    std::cout << "Hello from C++ with CMake on Windows" << std::endl;
    std::cout << "The sum is: " << add(4, 6) << std::endl;
    return 0;
}